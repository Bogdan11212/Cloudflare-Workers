{
  "name": "cloudflare-workers",
  "version": "0.0.0",
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20230518.0",
    "typescript": "^5.1.3",
    "vitest": "^0.32.2",
    "wrangler": "2.20.0"
  },
  "private": true,
  "scripts": {
    "start": "wrangler dev --local",
    "deploy": "wrangler publish",
    "test": "vitest",
    "start-persist": "wrangler dev --local --persist-to ./data",
    "replit-run-command": "npm run start-persist"
  }
}
